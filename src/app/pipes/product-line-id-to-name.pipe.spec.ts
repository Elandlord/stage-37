import { ProductLineIdToNamePipe } from './product-line-id-to-name.pipe';

describe('ProductLineIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductLineIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
